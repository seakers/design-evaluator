; --------------- RULE


(defrule MANIFEST::inherit-Instrument-Accuracy-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Accuracy ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Accuracy nil))

    =>

    (modify ?old (Accuracy ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Accuracy-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Accuracy#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Accuracy# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Accuracy# nil))

    =>

    (modify ?old (Accuracy# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Accuracy#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Accuracy2-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Accuracy2 ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Accuracy2 nil))

    =>

    (modify ?old (Accuracy2 ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Accuracy2-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-ADCS-requirements-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (ADCS-requirements ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(ADCS-requirements nil))

    =>

    (modify ?old (ADCS-requirements ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-ADCS-requirements-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-All-weather-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (All-weather ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(All-weather nil))

    =>

    (modify ?old (All-weather ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-All-weather-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Aperture#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Aperture# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Aperture# nil))

    =>

    (modify ?old (Aperture# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Aperture#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Angular-resolution#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Angular-resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Angular-resolution# nil))

    =>

    (modify ?old (Angular-resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Angular-resolution#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Angular-resolution-azimuth#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Angular-resolution-azimuth# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Angular-resolution-azimuth# nil))

    =>

    (modify ?old (Angular-resolution-azimuth# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Angular-resolution-azimuth#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Angular-resolution-elevation#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Angular-resolution-elevation# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Angular-resolution-elevation# nil))

    =>

    (modify ?old (Angular-resolution-elevation# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Angular-resolution-elevation#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-average-data-rate#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (average-data-rate# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(average-data-rate# nil))

    =>

    (modify ?old (average-data-rate# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-average-data-rate#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-average-power#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (average-power# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(average-power# nil))

    =>

    (modify ?old (average-power# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-average-power#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-band-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (band ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(band nil))

    =>

    (modify ?old (band ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-band-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-bandwidth#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (bandwidth# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(bandwidth# nil))

    =>

    (modify ?old (bandwidth# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-bandwidth#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-characteristic-orbit-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (characteristic-orbit ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(characteristic-orbit nil))

    =>

    (modify ?old (characteristic-orbit ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-characteristic-orbit-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Concept-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Concept ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Concept nil))

    =>

    (modify ?old (Concept ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Concept-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-cost#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (cost# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(cost# nil))

    =>

    (modify ?old (cost# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-cost#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Day-Night-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Day-Night ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Day-Night nil))

    =>

    (modify ?old (Day-Night ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Day-Night-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-developed-by-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (developed-by ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(developed-by nil))

    =>

    (modify ?old (developed-by ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-developed-by-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-dimension-x#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (dimension-x# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(dimension-x# nil))

    =>

    (modify ?old (dimension-x# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-dimension-x#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-dimension-y#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (dimension-y# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(dimension-y# nil))

    =>

    (modify ?old (dimension-y# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-dimension-y#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-dimension-z#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (dimension-z# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(dimension-z# nil))

    =>

    (modify ?old (dimension-z# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-dimension-z#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Doppler-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Doppler ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Doppler nil))

    =>

    (modify ?old (Doppler ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Doppler-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Field-of-regard#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Field-of-regard# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Field-of-regard# nil))

    =>

    (modify ?old (Field-of-regard# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Field-of-regard#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Field-of-view#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Field-of-view# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Field-of-view# nil))

    =>

    (modify ?old (Field-of-view# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Field-of-view#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Field-of-view-azimuth#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Field-of-view-azimuth# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Field-of-view-azimuth# nil))

    =>

    (modify ?old (Field-of-view-azimuth# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Field-of-view-azimuth#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Field-of-view-elevation#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Field-of-view-elevation# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Field-of-view-elevation# nil))

    =>

    (modify ?old (Field-of-view-elevation# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Field-of-view-elevation#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-focal-length#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (focal-length# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(focal-length# nil))

    =>

    (modify ?old (focal-length# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-focal-length#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-frequency#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (frequency# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(frequency# nil))

    =>

    (modify ?old (frequency# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-frequency#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Geometry-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Geometry ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Geometry nil))

    =>

    (modify ?old (Geometry ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Geometry-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-has-deployment-mechanism-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (has-deployment-mechanism ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(has-deployment-mechanism nil))

    =>

    (modify ?old (has-deployment-mechanism ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-has-deployment-mechanism-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-High-lat-sensitivity-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (High-lat-sensitivity ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(High-lat-sensitivity nil))

    =>

    (modify ?old (High-lat-sensitivity ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-High-lat-sensitivity-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution# nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution2-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution2 ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution2 nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution2 ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution2-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Along-track-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution-Along-track ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Along-track nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Along-track ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Along-track-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Along-track#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution-Along-track# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Along-track# nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Along-track# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Along-track#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Cross-track-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution-Cross-track ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Cross-track nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Cross-track ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Cross-track-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Cross-track#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Horizontal-Spatial-Resolution-Cross-track# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Cross-track# nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Cross-track# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Horizontal-Spatial-Resolution-Cross-track#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Illumination-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Illumination ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Illumination nil))

    =>

    (modify ?old (Illumination ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Illumination-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Imaging-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Imaging ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Imaging nil))

    =>

    (modify ?old (Imaging ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Imaging-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Intent-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Intent ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Intent nil))

    =>

    (modify ?old (Intent ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Intent-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Interferometry-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Interferometry ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Interferometry nil))

    =>

    (modify ?old (Interferometry ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Interferometry-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-mass#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (mass# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(mass# nil))

    =>

    (modify ?old (mass# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-mass#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-number-of-looks#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (number-of-looks# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(number-of-looks# nil))

    =>

    (modify ?old (number-of-looks# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-number-of-looks#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-C-band-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-C-band-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-C-band-channels nil))

    =>

    (modify ?old (num-of-C-band-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-C-band-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-Ka-band-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-Ka-band-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-Ka-band-channels nil))

    =>

    (modify ?old (num-of-Ka-band-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-Ka-band-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-Ku-band-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-Ku-band-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-Ku-band-channels nil))

    =>

    (modify ?old (num-of-Ku-band-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-Ku-band-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-L-band-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-L-band-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-L-band-channels nil))

    =>

    (modify ?old (num-of-L-band-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-L-band-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-mmwave-band-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-mmwave-band-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-mmwave-band-channels nil))

    =>

    (modify ?old (num-of-mmwave-band-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-mmwave-band-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-NIR-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-NIR-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-NIR-channels nil))

    =>

    (modify ?old (num-of-NIR-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-NIR-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-SWIR-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-SWIR-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-SWIR-channels nil))

    =>

    (modify ?old (num-of-SWIR-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-SWIR-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-TIR-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-TIR-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-TIR-channels nil))

    =>

    (modify ?old (num-of-TIR-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-TIR-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-UV-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-UV-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-UV-channels nil))

    =>

    (modify ?old (num-of-UV-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-UV-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-VNIR-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-VNIR-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-VNIR-channels nil))

    =>

    (modify ?old (num-of-VNIR-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-VNIR-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-of-X-band-channels-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-of-X-band-channels ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-of-X-band-channels nil))

    =>

    (modify ?old (num-of-X-band-channels ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-of-X-band-channels-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-pixels#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-pixels# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-pixels# nil))

    =>

    (modify ?old (num-pixels# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-pixels#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-pixels-along-track#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-pixels-along-track# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-pixels-along-track# nil))

    =>

    (modify ?old (num-pixels-along-track# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-pixels-along-track#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-num-pixels-cross-track#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (num-pixels-cross-track# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(num-pixels-cross-track# nil))

    =>

    (modify ?old (num-pixels-cross-track# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-num-pixels-cross-track#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-off-axis-angle-plus-minus#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (off-axis-angle-plus-minus# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(off-axis-angle-plus-minus# nil))

    =>

    (modify ?old (off-axis-angle-plus-minus# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-off-axis-angle-plus-minus#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-On-board-calibration-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (On-board-calibration ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(On-board-calibration nil))

    =>

    (modify ?old (On-board-calibration ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-On-board-calibration-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-peak-data-rate#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (peak-data-rate# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(peak-data-rate# nil))

    =>

    (modify ?old (peak-data-rate# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-peak-data-rate#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-peak-power#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (peak-power# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(peak-power# nil))

    =>

    (modify ?old (peak-power# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-peak-power#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Penetration-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Penetration ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Penetration nil))

    =>

    (modify ?old (Penetration ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Penetration-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Pointing-capability-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Pointing-capability ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Pointing-capability nil))

    =>

    (modify ?old (Pointing-capability ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Pointing-capability-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Pointing-requirements-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Pointing-requirements ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Pointing-requirements nil))

    =>

    (modify ?old (Pointing-requirements ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Pointing-requirements-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Polarimetry-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Polarimetry ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Polarimetry nil))

    =>

    (modify ?old (Polarimetry ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Polarimetry-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Priority-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Priority ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Priority nil))

    =>

    (modify ?old (Priority ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Priority-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Pulse-repetition-frequency#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Pulse-repetition-frequency# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Pulse-repetition-frequency# nil))

    =>

    (modify ?old (Pulse-repetition-frequency# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Pulse-repetition-frequency#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Radiometric-accuracy-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Radiometric-accuracy ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Radiometric-accuracy nil))

    =>

    (modify ?old (Radiometric-accuracy ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Radiometric-accuracy-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Radiometric-accuracy#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Radiometric-accuracy# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Radiometric-accuracy# nil))

    =>

    (modify ?old (Radiometric-accuracy# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Radiometric-accuracy#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-rms-system-instrument#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (rms-system-instrument# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(rms-system-instrument# nil))

    =>

    (modify ?old (rms-system-instrument# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-rms-system-instrument#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-rms-variable-measurement#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (rms-variable-measurement# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(rms-variable-measurement# nil))

    =>

    (modify ?old (rms-variable-measurement# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-rms-variable-measurement#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-scanning-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (scanning ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(scanning nil))

    =>

    (modify ?old (scanning ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-scanning-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-scanning-angle-plus-minus#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (scanning-angle-plus-minus# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(scanning-angle-plus-minus# nil))

    =>

    (modify ?old (scanning-angle-plus-minus# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-scanning-angle-plus-minus#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-dBZ#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-dBZ# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-dBZ# nil))

    =>

    (modify ?old (sensitivity-dBZ# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-dBZ#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-in-cirrus-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-in-cirrus ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-in-cirrus nil))

    =>

    (modify ?old (sensitivity-in-cirrus ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-in-cirrus-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-in-convective-clouds-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-in-convective-clouds ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-in-convective-clouds nil))

    =>

    (modify ?old (sensitivity-in-convective-clouds ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-in-convective-clouds-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-in-low-troposphere-PBL-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-in-low-troposphere-PBL ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-in-low-troposphere-PBL nil))

    =>

    (modify ?old (sensitivity-in-low-troposphere-PBL ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-in-low-troposphere-PBL-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-in-upper-stratosphere-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-in-upper-stratosphere ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-in-upper-stratosphere nil))

    =>

    (modify ?old (sensitivity-in-upper-stratosphere ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-in-upper-stratosphere-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-in-upper-troposphere-and-stratosphere-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-in-upper-troposphere-and-stratosphere ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-in-upper-troposphere-and-stratosphere nil))

    =>

    (modify ?old (sensitivity-in-upper-troposphere-and-stratosphere ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-in-upper-troposphere-and-stratosphere-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-over-lands-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-over-lands ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-over-lands nil))

    =>

    (modify ?old (sensitivity-over-lands ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-over-lands-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-over-oceans-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-over-oceans ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-over-oceans nil))

    =>

    (modify ?old (sensitivity-over-oceans ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-over-oceans-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-sensitivity-NEDT#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (sensitivity-NEDT# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(sensitivity-NEDT# nil))

    =>

    (modify ?old (sensitivity-NEDT# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-sensitivity-NEDT#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Spectral-region-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Spectral-region ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Spectral-region nil))

    =>

    (modify ?old (Spectral-region ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Spectral-region-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Spectral-resolution-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Spectral-resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Spectral-resolution nil))

    =>

    (modify ?old (Spectral-resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Spectral-resolution-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Spectral-resolution#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Spectral-resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Spectral-resolution# nil))

    =>

    (modify ?old (Spectral-resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Spectral-resolution#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Spectral-sampling-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Spectral-sampling ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Spectral-sampling nil))

    =>

    (modify ?old (Spectral-sampling ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Spectral-sampling-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Swath-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Swath ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Swath nil))

    =>

    (modify ?old (Swath ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Swath-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Swath#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Swath# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Swath# nil))

    =>

    (modify ?old (Swath# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Swath#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Swath2-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Swath2 ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Swath2 nil))

    =>

    (modify ?old (Swath2 ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Swath2-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Technology-Readiness-Level-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Technology-Readiness-Level ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Technology-Readiness-Level nil))

    =>

    (modify ?old (Technology-Readiness-Level ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Technology-Readiness-Level-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Temporal-resolution-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Temporal-resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Temporal-resolution nil))

    =>

    (modify ?old (Temporal-resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Temporal-resolution-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Temporal-resolution#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Temporal-resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Temporal-resolution# nil))

    =>

    (modify ?old (Temporal-resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Temporal-resolution#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Thermal-control-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Thermal-control ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Thermal-control nil))

    =>

    (modify ?old (Thermal-control ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Thermal-control-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-ThreeD-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (ThreeD ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(ThreeD nil))

    =>

    (modify ?old (ThreeD ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-ThreeD-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Vertical-Spatial-Resolution-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Vertical-Spatial-Resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Vertical-Spatial-Resolution nil))

    =>

    (modify ?old (Vertical-Spatial-Resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Vertical-Spatial-Resolution-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Vertical-Spatial-Resolution#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Vertical-Spatial-Resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Vertical-Spatial-Resolution# nil))

    =>

    (modify ?old (Vertical-Spatial-Resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Vertical-Spatial-Resolution#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-launch-date-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (launch-date ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(launch-date nil))

    =>

    (modify ?old (launch-date ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-launch-date-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-lifetime-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (lifetime ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(lifetime nil))

    =>

    (modify ?old (lifetime ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-lifetime-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-mission-architecture-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (mission-architecture ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(mission-architecture nil))

    =>

    (modify ?old (mission-architecture ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-mission-architecture-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-num-of-planes#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (num-of-planes# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(num-of-planes# nil))

    =>

    (modify ?old (num-of-planes# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-num-of-planes#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-num-of-sats-per-plane#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (num-of-sats-per-plane# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(num-of-sats-per-plane# nil))

    =>

    (modify ?old (num-of-sats-per-plane# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-num-of-sats-per-plane#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-orbit-altitude#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (orbit-altitude# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(orbit-altitude# nil))

    =>

    (modify ?old (orbit-altitude# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-orbit-altitude#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-orbit-anomaly#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (orbit-anomaly# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(orbit-anomaly# nil))

    =>

    (modify ?old (orbit-anomaly# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-orbit-anomaly#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-orbit-inclination-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (orbit-inclination ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(orbit-inclination nil))

    =>

    (modify ?old (orbit-inclination ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-orbit-inclination-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-orbit-RAAN-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (orbit-RAAN ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(orbit-RAAN nil))

    =>

    (modify ?old (orbit-RAAN ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-orbit-RAAN-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-orbit-string-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (orbit-string ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(orbit-string nil))

    =>

    (modify ?old (orbit-string ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-orbit-string-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Mission-orbit-type-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (MANIFEST::Mission (orbit-type ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(flies-in ?id) (factHistory ?fh)(orbit-type nil))

    =>

    (modify ?old (orbit-type ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Mission-orbit-type-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Accuracy-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Accuracy ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Accuracy nil))

    =>

    (modify ?old (Accuracy ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Accuracy-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Accuracy#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Accuracy# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Accuracy# nil))

    =>

    (modify ?old (Accuracy# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Accuracy#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Accuracy2-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Accuracy2 ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Accuracy2 nil))

    =>

    (modify ?old (Accuracy2 ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Accuracy2-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule MANIFEST::inherit-Instrument-Aperture-azimuth#-TO-Manifested-instrument
    (declare (no-loop TRUE))

    ?sub <- (DATABASE::Instrument (Aperture-azimuth# ?x&~nil)(Name ?id&~nil) )
    ?old <- (CAPABILITIES::Manifested-instrument(Name ?id) (factHistory ?fh)(Aperture-azimuth# nil))

    =>

    (modify ?old (Aperture-azimuth# ?x)(factHistory (str-cat "{R"(?*rulesMap* get MANIFEST::inherit-Instrument-Aperture-azimuth#-TO-Manifested-instrument) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-All-weather-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (All-weather ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(All-weather nil))

    =>

    (modify ?old (All-weather ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-All-weather-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-cold-regions#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (avg-revisit-time-cold-regions# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(avg-revisit-time-cold-regions# nil))

    =>

    (modify ?old (avg-revisit-time-cold-regions# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-cold-regions#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-global#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (avg-revisit-time-global# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(avg-revisit-time-global# nil))

    =>

    (modify ?old (avg-revisit-time-global# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-global#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-northern-hemisphere#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (avg-revisit-time-northern-hemisphere# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(avg-revisit-time-northern-hemisphere# nil))

    =>

    (modify ?old (avg-revisit-time-northern-hemisphere# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-northern-hemisphere#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-southern-hemisphere#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (avg-revisit-time-southern-hemisphere# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(avg-revisit-time-southern-hemisphere# nil))

    =>

    (modify ?old (avg-revisit-time-southern-hemisphere# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-southern-hemisphere#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-tropics#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (avg-revisit-time-tropics# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(avg-revisit-time-tropics# nil))

    =>

    (modify ?old (avg-revisit-time-tropics# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-tropics#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-US#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (avg-revisit-time-US# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(avg-revisit-time-US# nil))

    =>

    (modify ?old (avg-revisit-time-US# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-avg-revisit-time-US#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-band-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (band ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(band nil))

    =>

    (modify ?old (band ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-band-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Day-Night-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Day-Night ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Day-Night nil))

    =>

    (modify ?old (Day-Night ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Day-Night-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Field-of-view#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Field-of-view# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Field-of-view# nil))

    =>

    (modify ?old (Field-of-view# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Field-of-view#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-flies-in-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (flies-in ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(flies-in nil))

    =>

    (modify ?old (flies-in ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-flies-in-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Geometry-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Geometry ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Geometry nil))

    =>

    (modify ?old (Geometry ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Geometry-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-High-lat-sensitivity-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (High-lat-sensitivity ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(High-lat-sensitivity nil))

    =>

    (modify ?old (High-lat-sensitivity ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-High-lat-sensitivity-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution# nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution2-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution2 ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution2 nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution2 ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution2-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Along-track-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution-Along-track ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Along-track nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Along-track ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Along-track-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Along-track#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution-Along-track# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Along-track# nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Along-track# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Along-track#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Cross-track-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution-Cross-track ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Cross-track nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Cross-track ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Cross-track-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Cross-track#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Horizontal-Spatial-Resolution-Cross-track# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Horizontal-Spatial-Resolution-Cross-track# nil))

    =>

    (modify ?old (Horizontal-Spatial-Resolution-Cross-track# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Horizontal-Spatial-Resolution-Cross-track#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Illumination-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Illumination ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Illumination nil))

    =>

    (modify ?old (Illumination ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Illumination-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-image-distortion#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (image-distortion# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(image-distortion# nil))

    =>

    (modify ?old (image-distortion# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-image-distortion#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-launch-date-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (launch-date ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(launch-date nil))

    =>

    (modify ?old (launch-date ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-launch-date-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-lifetime-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (lifetime ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(lifetime nil))

    =>

    (modify ?old (lifetime ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-lifetime-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-mission-architecture-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (mission-architecture ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(mission-architecture nil))

    =>

    (modify ?old (mission-architecture ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-mission-architecture-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-num-of-planes#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (num-of-planes# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(num-of-planes# nil))

    =>

    (modify ?old (num-of-planes# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-num-of-planes#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-num-of-sats-per-plane#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (num-of-sats-per-plane# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(num-of-sats-per-plane# nil))

    =>

    (modify ?old (num-of-sats-per-plane# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-num-of-sats-per-plane#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-On-board-calibration-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (On-board-calibration ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(On-board-calibration nil))

    =>

    (modify ?old (On-board-calibration ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-On-board-calibration-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-orbit-altitude#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (orbit-altitude# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(orbit-altitude# nil))

    =>

    (modify ?old (orbit-altitude# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-orbit-altitude#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-orbit-anomaly#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (orbit-anomaly# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(orbit-anomaly# nil))

    =>

    (modify ?old (orbit-anomaly# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-orbit-anomaly#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-orbit-inclination-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (orbit-inclination ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(orbit-inclination nil))

    =>

    (modify ?old (orbit-inclination ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-orbit-inclination-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-orbit-RAAN-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (orbit-RAAN ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(orbit-RAAN nil))

    =>

    (modify ?old (orbit-RAAN ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-orbit-RAAN-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-orbit-string-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (orbit-string ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(orbit-string nil))

    =>

    (modify ?old (orbit-string ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-orbit-string-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-orbit-type-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (orbit-type ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(orbit-type nil))

    =>

    (modify ?old (orbit-type ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-orbit-type-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Penetration-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Penetration ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Penetration nil))

    =>

    (modify ?old (Penetration ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Penetration-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Pointing-capability-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Pointing-capability ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Pointing-capability nil))

    =>

    (modify ?old (Pointing-capability ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Pointing-capability-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Polarimetry-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Polarimetry ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Polarimetry nil))

    =>

    (modify ?old (Polarimetry ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Polarimetry-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Radiometric-accuracy-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Radiometric-accuracy ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Radiometric-accuracy nil))

    =>

    (modify ?old (Radiometric-accuracy ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Radiometric-accuracy-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Radiometric-accuracy#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Radiometric-accuracy# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Radiometric-accuracy# nil))

    =>

    (modify ?old (Radiometric-accuracy# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Radiometric-accuracy#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-rms-system-instrument#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (rms-system-instrument# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(rms-system-instrument# nil))

    =>

    (modify ?old (rms-system-instrument# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-rms-system-instrument#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity# nil))

    =>

    (modify ?old (sensitivity# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-cirrus-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-in-cirrus ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-in-cirrus nil))

    =>

    (modify ?old (sensitivity-in-cirrus ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-cirrus-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-convective-clouds-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-in-convective-clouds ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-in-convective-clouds nil))

    =>

    (modify ?old (sensitivity-in-convective-clouds ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-convective-clouds-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-low-troposphere-PBL-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-in-low-troposphere-PBL ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-in-low-troposphere-PBL nil))

    =>

    (modify ?old (sensitivity-in-low-troposphere-PBL ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-low-troposphere-PBL-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-upper-stratosphere-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-in-upper-stratosphere ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-in-upper-stratosphere nil))

    =>

    (modify ?old (sensitivity-in-upper-stratosphere ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-upper-stratosphere-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-upper-troposphere-and-stratosphere-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-in-upper-troposphere-and-stratosphere ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-in-upper-troposphere-and-stratosphere nil))

    =>

    (modify ?old (sensitivity-in-upper-troposphere-and-stratosphere ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-in-upper-troposphere-and-stratosphere-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-NEDT#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-NEDT# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-NEDT# nil))

    =>

    (modify ?old (sensitivity-NEDT# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-NEDT#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-over-lands-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-over-lands ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-over-lands nil))

    =>

    (modify ?old (sensitivity-over-lands ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-over-lands-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-sensitivity-over-oceans-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (sensitivity-over-oceans ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(sensitivity-over-oceans nil))

    =>

    (modify ?old (sensitivity-over-oceans ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-sensitivity-over-oceans-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-soil-penetration#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (soil-penetration# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(soil-penetration# nil))

    =>

    (modify ?old (soil-penetration# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-soil-penetration#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Spectral-region-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Spectral-region ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Spectral-region nil))

    =>

    (modify ?old (Spectral-region ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Spectral-region-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Spectral-resolution-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Spectral-resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Spectral-resolution nil))

    =>

    (modify ?old (Spectral-resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Spectral-resolution-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Spectral-resolution#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Spectral-resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Spectral-resolution# nil))

    =>

    (modify ?old (Spectral-resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Spectral-resolution#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Spectral-sampling-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Spectral-sampling ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Spectral-sampling nil))

    =>

    (modify ?old (Spectral-sampling ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Spectral-sampling-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Swath-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Swath ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Swath nil))

    =>

    (modify ?old (Swath ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Swath-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Swath#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Swath# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Swath# nil))

    =>

    (modify ?old (Swath# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Swath#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Swath2-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Swath2 ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Swath2 nil))

    =>

    (modify ?old (Swath2 ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Swath2-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Temporal-resolution-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Temporal-resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Temporal-resolution nil))

    =>

    (modify ?old (Temporal-resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Temporal-resolution-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Temporal-resolution#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Temporal-resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Temporal-resolution# nil))

    =>

    (modify ?old (Temporal-resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Temporal-resolution#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-ThreeD-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (ThreeD ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(ThreeD nil))

    =>

    (modify ?old (ThreeD ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-ThreeD-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Vertical-Spatial-Resolution-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Vertical-Spatial-Resolution ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Vertical-Spatial-Resolution nil))

    =>

    (modify ?old (Vertical-Spatial-Resolution ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Vertical-Spatial-Resolution-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-Vertical-Spatial-Resolution#-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (Vertical-Spatial-Resolution# ?x&~nil)(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(Vertical-Spatial-Resolution# nil))

    =>

    (modify ?old (Vertical-Spatial-Resolution# ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-Vertical-Spatial-Resolution#-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)


(defrule CAPABILITIES::inherit-Manifested-instrument-spectral-bands-TO-Measurement
    (declare (no-loop TRUE))

    ?sub <- (CAPABILITIES::Manifested-instrument (spectral-bands $?x&:(> (length$ $?x) 0))(Name ?id&~nil) )
    ?old <- (REQUIREMENTS::Measurement(taken-by ?id) (factHistory ?fh)(spectral-bands nil))

    =>

    (modify ?old (spectral-bands ?x)(factHistory (str-cat "{R"(?*rulesMap* get CAPABILITIES::inherit-Manifested-instrument-spectral-bands-TO-Measurement) " " ?fh " S"(call ?sub getFactId) "}")))
)



